# Deprecated on purpose.
# Do not schedule Docker runs from a mutable source checkout.
# Use `docker/setup.sh` or `docker/setup.ps1` to validate the image and then
# install a host scheduler command that runs `docker run ... ad-password-sentinel:local run`
# against root-owned or administrator-owned config and secret paths.
